{
  "name": "acceptance-harness",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Standalone content-assertion acceptance harness. Drives the running frontend app and cross-checks rendered text/DOM against the real data/export/*.json files. Lives outside frontend/ on purpose -- see README.md.",
  "scripts": {
    "start": "node harness.mjs"
  },
  "devDependencies": {
    "playwright": "^1.62.0"
  }
}
