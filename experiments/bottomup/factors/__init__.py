"""Factor batch 1 -- registry #19, #20, #28, #13.

Design fixed in `docs/ranking/factor-batch-1-precommit.md`, committed as d546cff
BEFORE any arm was fitted. Every arm here is one change away from the primary
reported in `docs/ranking/component-model-rb-qb-te-pass-1.md`, and the primary is
re-run inside the same process so the comparison is paired by season.
"""
