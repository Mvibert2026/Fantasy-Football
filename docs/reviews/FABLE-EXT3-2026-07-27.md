# FABLE — session 4 (final before absence), 2026-07-27

STATUS: COMPLETE — both jobs delivered, wrap done, H3 untouched by instruction
LAST UPDATED: session end

Branch: `fable/ext-2026-07-27` (continuing from session 3's `d635588`).
Budget: ~2 hours. Founder's mandate for this session, verbatim priorities:

1. **Job 1 (first): R3 + N3** — the week-indexed projection object, specced fully enough
   that a sonnet agent can build it without me. Written as an ADR-E amendment with a
   sonnet-executable work order. Three consumers are blocked on its shape during my absence:
   T4's suspension games-adjustment, bye-week cost, and N3's leverage weights.
2. **Job 2: the rookie hypothesis** — the registered blind spot of the V3/V5 feature family
   and the leading remaining candidate for the RB consensus gap. Registration lands BEFORE
   experiment code exists, committed. If the run does not finish, the registration alone is
   the deliverable.
3. **Before stopping:** amend `ACTION-PLAN-2026-08.md`, update this landing note. Do NOT
   start H3 — leave it clean.

## What I have done

- This landing note created (this file), commit `c3937b5`.
- **Job 1 COMPLETE.** ADR-E baseline committed onto the branch (`8b9a79d`, byte-identical to
  the master untracked copy), then amendment **E-A1** committed (`324469d`):
  `docs/adr-drafts/ADR-E-bottom-up-projection-framework.md` §A1.1–A1.7. The week-indexed
  projection object (R3) and the week-leverage weights (N3), specified once with every
  consumer named, valid interim forms stated, and a sonnet work order (R3-A / R3-B / R3-C)
  executable without me. Master untracked copy synced byte-identical.

- **V7 registered** before any code (`5af349e` — the registration section below), then
  **implemented** (`bf1c2d1`): `rookie_draft_capital` + PFR crosswalk in `data.py`, the three
  features in `situation.py`, `--rookies` in `run.py`, 7 new tests (16 pass in
  `tests/test_situation_features.py`, including crosswalk totality 2003–2024).

- **V7 run to completion, both arms** (`results/{usage,long}_v7.json`); results read
  against the registration in `experiments/bottomup/REPORT.md` (session-4 appendix) and
  `VARIANTS.md` (tally: 7 configurations). Full suite: **539 passed, 1 failed — the one
  failure is T3, red by design on the live JAC/LAR bye defect** (session-2/3 baseline
  532+1, plus this session's 7 new tests).

- **Wrap complete:** `ACTION-PLAN-2026-08.md` Amendment 2 (D4 resolved; rookie channel off
  the return-week list; playoff-week check folded into D3; integrator merge notes;
  calibration prior) — now tracked on the branch (`16d62a5`) with the master untracked copy
  synced byte-identical. Founder requests captured as **FR-009** (week-indexed projection)
  and **FR-010** (registration-before-code as the deliverable standard) in the master
  working copy's `docs/founder-requests.md`.

## What I was doing when this was last updated

Nothing in flight — session closed cleanly.

## What I did not reach

- **H3: not started, left clean, per the founder's explicit instruction.** It remains the
  gate on any confirmatory run.
- The F-BOTTOMUP-CORE registration package itself — deliberately, per the plan: it belongs
  in the return-week discussion with the red-team pass, after H3 lands.

## Headline findings

1. **The rookie hypothesis is FALSIFIED as registered.** V7 (V5 + same-position
   rookie-arrival draft capital) improved RB on neither co-primary (Δτ vs B1
   +0.057→+0.054; VBD +0.032→+0.018) and the RB consensus gap did not move
   (−0.110→−0.112). Rookie-driven situation change, as expressible through draft capital,
   joins vacated opportunity as *eliminated* explanations of the RB gap — the two channels
   the ranking-design review ranked highest. The working favourite is now "no single
   channel": ECR as an aggregate of many small edges. Product consequence already adopted
   (consensus-anchored board, labelled overlay). **Recommendation: stop spending on gap
   hypotheses; the next registered spend is the confirmatory F-BOTTOMUP-CORE run on V5.**
2. **Calibration prior, now strong enough to state as standing:** four of five registered
   prediction sets across sessions 3–4 were materially wrong, every miss over-crediting a
   situation story. Price situation narratives at half their intuitive weight before
   registering. (The discipline converted every miss into a clean elimination — V7 cost one
   session and produced a definitive answer precisely because it was registered first.)
3. **(Job 1, design)** T4's suspension games-adjustment needs no bespoke formula: with the
   week-indexed vector (ADR-E amendment E-A1, commit `324469d`), suspension cost = the
   difference of two leverage-weighted integrals, and each structural-zero week reduces
   expected games by exactly q_p (identity I1). The three blocked consumers (T4 valuation,
   bye cost, N3 weights) all read from §A1 now; sonnet work orders R3-A/R3-B are executable
   without me.
4. **(Job 1, flag for founder)** `league_config.py` says `playoff_weeks=(16,17)`; the
   founder's session-4 note says championships are weeks 15–17. One of these is wrong; the
   N3 utility reads config, so it is a one-line fix once verified against the live platform
   (T2-adjacent, one minute). Do not let both numbers circulate.

## Next steps for the project

Ranked; each actionable without me. The week's critical path is unchanged
(`ACTION-PLAN-2026-08.md`: W8 backup → T1 half-PPR → T5 freshness → T4 suspensions → T2
scoring verification).

1. **Founder, Day 1 (~15 min):** unchanged D1–D6; the D3 platform visit now also captures
   the playoff week boundaries (16–17 vs 15–17 — one screenshot answers both T2 and this).
2. **Integrator, Day 1 (~0.5):** merge `fable/ext-2026-07-27` per D2. New wrinkle: delete
   the two untracked master copies (`docs/adr-drafts/ADR-E-...md`,
   `docs/reviews/ACTION-PLAN-2026-08.md`) first — they are byte-identical to the branch's
   tracked versions and git will refuse the merge while they exist. Suite baseline:
   539 + 1 by-design (T3).
3. **Backend, Day 5 (~0.25):** R3-A `src/week_leverage.py` per ADR-E §A1.7 — this
   supersedes the plan's original item 5.3 spec (adds mean-1 normalisation + provenance).
4. **Backend, weekend if capacity (~1):** R3-B `src/week_vector.py` (after the T4-interim
   fixture) — unblocks suspension valuation and bye cost mechanically.
5. **Return week (Fable):** red-team + freeze F-BOTTOMUP-CORE on V5 (+ R5 + C3), gated on
   H3 landing first. **No further RB-gap hypotheses** — two clean eliminations is enough;
   the labelled-overlay product posture stands.

## What I would do with more budget

Pre-draft the F-BOTTOMUP-CORE registration package as an explicit DRAFT-NOT-REGISTERED
skeleton (V5 features, C3 rookie-universe options, R5 calibration family, m and BH
structure), so the return-week session spends its budget red-teaming rather than drafting.
I deliberately did not start it tonight: the plan reserves it for the session that can also
verify H3 and run the red-team pass, and a rushed half-registration is worse than none.

---

## Registration — V7, rookie-arrival draft capital (frozen BEFORE any V7 code exists)

Written 2026-07-27, session 4. Committed to the branch before any implementation, so the
ordering is in git, not on trust. **If this session is cut off before the run, this
registration is a complete deliverable — a sonnet agent executes it as written, changing
nothing.** Metric, universe, baselines, fold sets, and reporting are UNCHANGED from the
session-2 prototype registration (`fable-ranking-design-2026-07-27.md`) as amended for V5
(`FABLE-EXT2-2026-07-27.md`). Data facts below are metadata reads, not fits: `draft_picks`
covers 1980–2026; `pick` is the overall pick number (1–257, verified against 2024 rounds);
skill-position codes present 2002–2024 are QB/RB/WR/TE (+42 FB picks, excluded below).

### The mechanism, stated exactly

Rookies are excluded from the v0 universe by registration (prior-season finish defines it),
so **vacated opportunity flowing TO a rookie is invisible to every variant so far**: V3/V5's
arrival shares are built from t−1 production, which rookies do not have. A team that loses
180 carries and drafts a first-round RB shows high vacated share and zero arrived
competition — the model credits the remaining veterans with an open backfield that is
actually spoken for. ECR prices the rookie; B1 and our model do not. This was the registered
blind spot of the V3/V5 family and is the leading remaining hypothesis for the RB consensus
gap (−0.110, unmoved by V5). Draft capital is the natural carrier: it is public in April,
production-independent, and fully pre-season (no look-ahead).

### V7 — V5 + rookie-arrival draft capital (one configuration)

Base: **V5 exactly** (self-excluded vacated shares, composition pipeline at all four
positions, both arms, same folds: long 2002–2024, usage 2012–2024). Three features appended
to the shared S1 vector (per-position ridges weight them freely):

1. `rook_cap_same` — Σ over rookies drafted in the season-t draft by p's current franchise
   at p's position: `1/sqrt(overall_pick)`. Position match is exact code equality on
   {QB, RB, WR, TE}; FB is excluded (declared: 42 picks over 23 drafts, not modern workload
   competition).
2. `rook_top64_same` — indicator that any such pick is ≤ 64 overall (rounds 1–2).
3. `rook_cap_x_vac` — `rook_cap_same ×` the position-relevant **self-excluded** vacated
   share from V5: `vac_att_share` for QB, `vac_carry_share` for RB, `vac_rec_share` for
   WR/TE. This is the mechanism term: vacated volume in the presence of high rookie capital
   flows to the rookie, not to returning veterans.

**Current-franchise assignment:** identical to V5 (weeks-1–4 modal canonical team), carrying
V3/V5's registered look-ahead disclosure unchanged. No new look-ahead enters: the season-t
draft is complete before ADP forms. A universe player can never be a season-t draftee
(universe requires a t−1 finish), so the V5 self-exclusion issue cannot arise here.

**Team-code crosswalk (required, structural):** `draft_picks` uses PFR-era codes; the
experiment uses modern canonical codes. Declared mapping: GNB→GB, KAN→KC, NOR→NO, NWE→NE,
SFO→SF, TAM→TB, SDG→LAC, STL→LA, OAK→LV, LVR→LV, LAR→LA; identity otherwise (JAX stays JAX).
A test must assert every crosswalked `draft_picks` team code for seasons ≥ 2003 appears in
the canonical team set observed in `player_weekly_stats`; a code that does not resolve is a
build failure, not a silent zero (the T3/T9 lesson).

### Registered predictions (direction, magnitude, positions — the point of this section)

- **Direction:** improvement concentrates at **RB** — rookie RB usurpation is the most
  common and most immediate same-position competition (first-round RBs take over backfields
  in year 1). WR smaller and positive (rookie WRs ramp slower; target competition is
  diffuse). TE ≈ unchanged (rookie TEs are rarely productive in year 1).
- **Magnitude, RB (usage arm, vs V5's numbers):** Δτ_b vs B1 improves from +0.057 by
  **+0.010 to +0.030**; consensus gap (2021–2024 descriptive, −0.110 at V5) narrows by
  **0.02 to 0.05** — a fifth to a half. Co-primary (VBD-capture) positive, same pattern.
- **Magnitude, WR/TE:** WR |Δτ_b| ≤ 0.02 (positive side); TE |Δτ_b| ≤ 0.01.
- **QB: descriptive only, no prediction stakes.** QB modelling is closed (six
  configurations, two sessions); V7's QB numbers are reported for completeness and do not
  reopen it. A rookie-QB effect on incumbents is real but is an availability channel, and
  availability channels at QB are exactly where V3 leaked.
- **Falsified if:** RB fails to improve on BOTH co-primaries vs V5 AND the RB consensus gap
  does not narrow by ≥ 0.02. Then rookie-driven situation change **as expressible via
  same-position draft capital** is eliminated as the explanation of the RB gap, and is
  reported as an elimination. Contradiction guard (unchanged from V3): a material WR jump
  (> +0.03) alongside a flat RB contradicts the mechanism reading, not rescues it.
- **What remains if falsified, stated now:** (a) rookie *inclusion* — rookies themselves
  mispriced, the C3 universe arm, still reserved for the return-week registration;
  (b) ECR's non-box-score information (camp/beat reports, holdout and injury pricing —
  partially the T-series' job, not a model feature); (c) coaching/scheme change (R2, gated
  on data); (d) no single channel — ECR as an aggregate of many small edges, in which case
  no feature family closes the gap and the labelled-overlay product posture (D7) is the
  correct end state, not a way station.
- **Ceiling:** session-2 Q4 bands govern unchanged; any season-points R² > 0.40 triggers
  the ADR-E §8 audit posture before any number is reported.

**Decision rule:** V7 replaces V5 as the carried candidate only if RB improves on ≥ 1
registered co-primary vs V5 without degrading WR/TE beyond −0.02 Δτ_b and with no audit
flag outstanding. The consensus-gap movement is the mechanism verdict (descriptive, n=4,
no p-value — per standing language rules), not the carry criterion.

**Variant accounting:** V7 is this session's only registered configuration. Running count:
V1, V2, F1 (session 2) + V3, V4, V5, V6 (session 3) + **V7 (session 4)**. Any further
configuration requires its own registered entry BEFORE running.

### Sonnet execution notes (only if I do not finish the run)

- `experiments/bottomup/data.py`: add `rookie_draft_capital(season)` reading
  `draft_picks` (season, team, position, pick) with the crosswalk above; canonicalise via
  the same `canon_team` convention; return `{(canon_team, position): [overall picks]}`.
- `experiments/bottomup/situation.py`: append the three features; reuse the V5
  current-franchise assignment and self-excluded vacated shares already computed there.
- `experiments/bottomup/run.py`: variant flag `--v7`; both arms; results JSONs alongside
  the V5/V6 ones; REPORT.md session-4 appendix; VARIANTS.md tally 8 entries.
- Tests: crosswalk totality (per above); a synthetic fixture where a team drafts an RB at
  overall 10 → incumbent RB's `rook_cap_same = 1/sqrt(10)`, `rook_top64_same = 1`, and
  the interaction equals capital × that team's vacated carry share; a no-rookie team →
  all three features 0; FB pick → 0.
- The worktree DB copy at `data/nfl.db` is required for the full suite (session-3 note).

---

*Working notes appended below as they land, so a cutoff at any moment leaves the trail
complete.*
