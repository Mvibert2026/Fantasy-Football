# FABLE — final-session log, 2026-07-27 (session 3)

STATUS: COMPLETE — Priority 0 delivered; Priority 1 (R1) run to a decisive split verdict;
Priority 2 (QB) answered and closed; Priority 3 (H3) consciously not started, left clean.
LAST UPDATED: session end

## Headline findings (full numbers: experiments/bottomup/REPORT.md, session-3 appendix)

1. **R1 splits.** Clean vacated/arrived-opportunity features (V5) are the model's best
   result yet vs last-season-rank at RB (Δτ +0.057 [+0.018,+0.095], VBD +0.032, 10/13
   folds — hitting the magnitude band registered before any fit) and TE (VBD +0.073). But
   the consensus gap moved NOWHERE (RB −0.110 → −0.110). Vacated opportunity is real signal
   the model lacked; it is NOT the missing channel behind the consensus gap. The leading
   hypothesis for that gap is eliminated as tested (rookie blind spot caveat registered).
2. **A leak was caught by the discipline.** V3's spectacular QB result (Δτ −0.108 → −0.018)
   was ~entirely a self-vacated availability leak (no-early-appearance players encode "not
   playing in September" in their own features; ~1 universe QB per fold). The clean variant
   erased it. Without registration-before-fit this would have shipped as "QB solved."
3. **QB is closed.** Six configurations over two sessions — composition, cap-raise, leaky
   situation, direct ridge with prior points, clean both ways — and none beats ranking QBs
   by last season's points. Mechanism found (descriptive): QB volume stability is ordinary
   (y2y attempts/g r=0.62 ≈ RB/WR); the persistence lives in the efficiency+rushing bundle
   S2's caps shrink away, and no estimator at n≈20/fold recovers it better than the raw
   points rank. The PM's hypothesis: refuted in mechanism, confirmed in consequence.
4. **Hybrid board answered** (the Priority-2 product question): consensus ≥ our best clean
   model at every position, so the 2026 board stays consensus-anchored; the bottom-up model
   ships as a LABELLED independent overlay at RB/WR/TE only (where it beats naive
   persistence and sits within noise of ECR), not at QB. Mixed provenance in the primary
   board is acceptable only with per-row source labels and the RB −0.110 printed on it.
5. **Prediction scorecard, honestly:** three of the four registered prediction sets this
   session were materially wrong. That is the discipline working — it caught the leak and
   blocked a false headline — and it is also a calibration datum about my own priors on
   this project: I over-credit situation stories at QB.

## Done so far

- **Priority 0 — `ACTION-PLAN-2026-08.md` written first, before any analysis, per mandate.**
  Day-by-day sequence (W8 backup first; T1 critical path Day 2; T9/T5 Day 3; T4/T6/W1 Day 4;
  T8/N1/N3/H3 Day 5), founder decision list D1–D6, explicit deprioritisations, failure
  branches, what waits for Fable. The hybrid-board decision is deliberately deferred to this
  session's amendment.
- **Priority 1 — V3 registered (commit `678615c`), built (`f466aaa`), run.** Headline: the
  registered RB prediction FAILED (RB unchanged on both co-primaries; falsification rule
  triggered in the usage arm); QB improved far beyond prediction (Δτ vs B1 −0.108→−0.018,
  VBD −0.224→−0.079, consensus gap −0.223→−0.129) — but a self-vacated availability leak was
  identified and sized (~1 universe QB per fold), so V3 is downgraded to an upper-bound
  reference and V5 (clean) registered as the only carry candidate (commit `1c16ab4`).
- **Priority 2, part 1 — the "why does QB lose" question answered descriptively:** the PM's
  team-stable-volume hypothesis is refuted as stated. QB attempts/g y2y stability (r=0.62)
  equals RB/WR; what is QB-specific is that prior volume alone barely ranks players
  (τ +0.096 vs +0.275 for prior points) — the persistent signal is the efficiency+rushing
  bundle that S2's caps deliberately shrink toward the mean. The composition loses at QB
  because it discards that bundle, not because volume is stable. (V2's cap-raise already
  showed the TD slice alone is not it.)

## Registration — V3 (R1 vacated opportunity) and V4 (QB arm), frozen BEFORE any fitting

Written 2026-07-27, session 3, before any V3/V4 code was run. Committed to the branch before
the first fit so the ordering is in git, not on trust. Metric, universe, baselines, fold sets,
and reporting are UNCHANGED from the session-2 registration
(`fable-ranking-design-2026-07-27.md`, "Prototype registration"). Data facts used below
(team-code eras, weeks-1–4 coverage) are metadata reads, not fits.

### V3 — vacated/arrived opportunity features (work order R1)

**Franchise canonicalisation** (measured this session): the DB uses modern codes from 2003
onward; map `{OAK→LV, SD→LAC, STL→LA, JAC→JAX}` applies only to 1999–2002 rows, i.e. only to
long-arm folds whose feature season is ≤ 2002.

**Current-franchise assignment** for player p, target season t: modal canonical team over p's
REG appearances in **weeks 1–4 of season t**, reading only (player_id, team, week) — never a
production quantity. If p has no weeks-1–4 appearance: current := his t−1 franchise,
`changed_team` := 0, and p counts as NOT retained in others' vacated shares.

**Registered look-ahead disclosure:** weeks-1–4 roster membership is target-season
information. It is the approximation the R1 spec blesses ("week-1 roster… mild look-ahead"),
widened to weeks 1–4 to avoid misclassifying short absences as departures. It can advantage
the model over B1 precisely where the market also knew (August injuries, retirements,
holdouts). Every report of V3 carries this flag.

**Registered blind spot:** arrivals are visible only via t−1 production, so **rookies are
invisible to arrival shares** — a team drafting a first-round RB shows zero arrived
competition. This mutes the measurable situation effect at RB and QB; a null result does not
eliminate rookie-driven situation change as a channel.

**Six features appended to the shared S1 vector** (per-position ridges weight them freely):
1. `changed_team` (0/1)
2. `vac_rec_share` — share of current franchise's t−1 receiving volume (targets in the usage
   arm, receptions in the long arm — same availability rule as `rec_vol`) produced by players
   not retained by that franchise in t
3. `vac_carry_share` — same for carries
4. `vac_att_share` — same for pass attempts
5. `arr_rec_share` — t−1 receiving volume produced elsewhere by arrivals at the current
   franchise (excluding p himself), over the franchise's t−1 receiving volume
6. `arr_carry_share` — same for carries

Zero denominators (expansion HOU 2002) → share 0. Both arms run, same folds as V1
(long 2002–2024, usage 2012–2024). One configuration; no tuning passes.

**Registered predictions (the point of this section):**
- Direction: improvement concentrates at **RB**; smaller at **QB**; **WR/TE ≈ unchanged**
  (|Δ| ≤ 0.02 on Δτ_b vs B1). This is the session-2 consensus-gap fingerprint read forward.
- Magnitude, RB: Δτ_b vs B1 from +0.041 to **+0.05..+0.08**; consensus gap (2021–2024
  descriptive, currently −0.110) narrows by **one-third to one-half**.
- Magnitude, QB: gap vs B1 (−0.108) shrinks toward **−0.06..−0.09** but stays negative;
  consensus gap (−0.223) narrows by ≤ one-quarter. Low confidence — QB situation is
  scheme/coaching more than vacated volume, and the rookie blind spot bites here too.
- Co-primary (VBD-capture): same directional pattern; RB positive.
- **Falsified if:** RB improves on NEITHER co-primary vs V1 AND the RB consensus gap does not
  narrow. Then the vacated-opportunity form of the situation-information diagnosis is refuted
  and is reported as such — a valuable elimination, not a disappointment. A material WR jump
  (> +0.03) alongside a flat RB would *contradict* the fingerprint reading, not rescue it.
- Ceiling: session-2 Q4 bands govern unchanged; any season-points R² > 0.40 triggers the §8
  audit posture.
- Decision rule: V3 replaces V1 as the carried candidate only if RB improves on the
  registered co-primaries without degrading WR/TE beyond −0.02 Δτ_b.

### V4 — QB arm (Priority 2), registered now so it is not conditioned on V3's result

**Change:** for **QB only**, replace the S1→S2→S3 composition with a direct ridge on season
points; features = the QB slice of the V3 vector plus `prior_points`, `prior_ppg`,
`prior2_ppg` (0 when t−2 absent). RB/WR/TE pipelines unchanged from V3 (reported for
completeness; not V4's question). Both arms, same folds. One configuration.

**Registered predictions:**
- The PM's untested hypothesis (QB scoring ≈ team-stable passing volume, so last-season-rank
  is an unusually strong baseline and usage features add variance without signal) predicts:
  the direct arm with prior points **closes most of the QB gap to B1** (from −0.108 to
  within ±0.04) but does **not** beat B1 by a CI-clear margin.
- If the direct arm still loses to B1 CI-clear at QB, the strong form of the hypothesis holds
  and the product answer is consensus/last-season rank at QB (the hybrid board).
- If it *beats* B1 clearly, the session-2 conclusion "no at QB" was an artifact of the
  composition (games × components), not of QB predictability — say so.
- Diagnostics accompanying (descriptive, not variants): per-position year-over-year points
  and attempts stability within the universe; τ_b of prior-ppg-rank at QB vs B1's
  prior-points-rank.

**Variant accounting:** V3 and V4 are the session's only two model configurations. Any
further configuration requires its own registered entry BEFORE running. Continuing count:
V1, V2, F1 (session 2) + V3, V4 (session 3).

### AMENDMENT (2026-07-27, session 3, after V3 ran and before V5/V6 ran) — V5 and V6

**What V3 showed (against the registration):** the predicted RB gain did not materialise
(usage arm: Δτ vs B1 +0.041→+0.040, VBD +0.016→+0.011, consensus gap −0.110→−0.108 — the
registered RB falsification rule triggered), while QB — registered low-confidence — improved
far more than predicted (Δτ vs B1 −0.108→−0.018; VBD −0.224→−0.079; consensus gap
−0.223→−0.129). Full numbers in REPORT.md.

**The leakage concern that forces V5:** as registered, a player with no weeks-1–4 appearance
contributes his own t−1 production to the vacated pool of the franchise he himself is
assigned to. His own `vac_*_share` therefore encodes "this player is not playing early in
season t" — an availability signal. Mostly the market knew it too (retirements, August
injuries), but it includes post-draft information (weeks-1–4 injuries, camp benchings
resolved after ADP). Measured class size, usage folds: QB 14/260 (5.4%), RB 59/585, WR
53/780, TE 22/260 — at QB that is ~1 universe player per fold in a 20-player universe, enough
to move τ_b materially. The V3 QB gain is therefore suspect until decomposed.

**V5 — self-excluded vacated shares (the clean variant):** identical to V3 except each
player's own contribution is removed from the vacated pool used in HIS OWN vac features
(numerator only; denominators and every other player's features unchanged; arrivals already
excluded self). This removes the self-availability channel while keeping the beneficiary
channel (vacated by *others*).

*Registered predictions:* if V3's QB gain is mostly self-leakage, V5's QB Δτ vs B1 falls at
least halfway back toward V1 (below −0.06); if the gain is genuine situation signal
(new-team QBs, room turnover), V5 retains most of it (−0.04 or better). Point prediction:
QB lands in **[−0.07, −0.02]** usage arm; RB/WR/TE within ±0.015 of V3. Decision rule: V5,
not V3, is the only carry candidate — a variant with a known intra-season availability leak
is not shippable regardless of its score; V3 becomes the upper-bound reference.

**V6 — V5 + QB-direct (the hybrid-board candidate):** V4's QB-direct arm re-run on V5's
clean feature set (the registered V4 text predates the leak finding and binds to V3's
features; V6 is the same design on the clean features). One configuration.

*Registered predictions:* QB Δτ vs B1 in **[−0.04, +0.04]**, not CI-clear either way at
n=13; the PM-hypothesis strong form (direct arm with prior points closes most of the gap)
transfers from V4's registration. RB/WR/TE identical to V5 by construction (QB-only change).

**Diagnostics result (descriptive, already run, registered in the V4 block):** the PM's
hypothesis is REFUTED as stated — QB volume stability is not unusual (y2y attempts/g r=0.62
≈ RB 0.63, WR 0.62); what distinguishes QB is that prior volume alone barely predicts points
(τ +0.096 vs prior-points +0.275), i.e. QB points persistence lives in the efficiency and
rushing bundle that S2's caps (0.60/0.20) deliberately shrink toward the mean. The
composition loses at QB because it discards persistent QB-specific efficiency signal, not
because volume is team-stable. (V2's cap-raise tested only the TD slice and already failed;
prior-points-as-feature is the honest carrier of the whole bundle — hence V4/V6.)

Final session-3 variant count: **V3, V4, V5, V6 — four configurations**, every one
registered before its run.

## What I built (branch `fable/ext-2026-07-27`, continuing from session 2's `00ae336`)

Commits, in order:
- `678615c` — V3/V4 registration frozen and committed BEFORE any code or fit existed.
- `f466aaa` — implementation: `situation.py` (vacated/arrived features), `early_rosters`
  + `canon_team` in `data.py`, V4 QB-direct in `model.py`, flags in `run.py`,
  `tests/test_situation_features.py` (8 tests).
- `1c16ab4` — V3 results + `qb_diagnostics.py` + V5/V6 registration (after V3 ran, before
  V5/V6 ran).
- `1ddcf29` — V5 implementation (self-exclusion) + 1 test.
- `16f76a7` — V4/V5/V6 results, REPORT.md session-3 appendix, VARIANTS.md final tally, this
  log. (A trailing doc-only commit syncs this hash reference.)

Files: `experiments/bottomup/situation.py`, `qb_diagnostics.py`, six new result JSONs,
edits to `data.py`/`model.py`/`run.py`, `tests/test_situation_features.py` (9 tests).

**Merge assessment, per item:** everything this session is additive inside
`experiments/bottomup/` and `tests/` — safe to merge as-is, same as session 2's assessment
for the prototype. No production `src/` or `frontend/` files touched this session. The
session-2 assessment (T3 red by design; frontend/e2e needs one frontend look) stands
unchanged.

**Test suite: 532 passed, 1 failed (99s) — the single failure is T3, red on the live
JAC/LAR bye defect BY DESIGN, unchanged from session 2** (whose baseline was 523+1; the +9
are `test_situation_features.py`). The full worktree suite requires the DB visible at the
worktree's own `data/nfl.db` (session 2's arrangement; a local gitignored copy now sits
there — without it, 21 DB-dependent tests fail on an auto-created empty stub, which is what
a first naive run of this suite in a fresh worktree will show).

## Consciously not reached

1. **H3 (prereg-gate entrypoint wiring) — not started, left clean, per the mandate's own
   instruction.** It remains sequenced as a PREREQUISITE of ADR-E's first confirmatory run
   (Day-5 optional slot in the action plan; must land before F-BOTTOMUP-CORE registers).
   Nothing partial exists.
2. Rookie-inclusion arm / rookie-situation features — now the leading remaining hypothesis
   for the RB consensus gap; queued for the return-week registration discussion.
3. W1/W2 and the remaining table-stakes checks — owned in the action plan, not Fable work.

## What needs Fable on return (~2026-08-03)

1. Red-team the ADR-E §4.1 amendment set (V5 feature group + R3 + R5 + C3) and the
   F-BOTTOMUP-CORE registration; verify H3 landed first.
2. The rookie-situation channel: design a registered test using draft capital (visible
   pre-season, production-independent).
3. Read the week's T1/T2 outcomes — if the live scoring page contradicts §7's
   reconstruction, every session-2/3 number rescales (the deltas should survive; the
   absolute points will not).
4. D7 (hybrid board) if the founder wants discussion rather than the written
   recommendation.
