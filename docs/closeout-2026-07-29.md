# Closeout — 2026-07-29

**Milestone closeout.** First full one since the project moved to cloud sessions and since the PM
moved inside the repo. Run by the PM, serially, per `docs/pm/CLOSEOUT.md`.

---

## 1 · Git state

Clean. Nothing uncommitted, **no stashes**, local and remote agree. `main` and the working branch
both carry the day's work.

**Unmerged branches — four, each with a named reason:**

| Branch | Status |
|---|---|
| `backend/mock-calibration-kickers` | Rescued as-found, **never reviewed**. Carries a competing ADR-054 and ADR-055 — see §4. Do not merge without reading it. |
| `claude/cloud-path-rehearsal-kafx7m` | Its findings were adopted and its corrections landed. Branch itself superseded. |
| `claude/pm-agent-availability-26ww7z` | Predecessor PM branch, superseded. |
| `docs/phase3-chain2-claude-md-agents` | Predates the agent rewrite; superseded. |

**Named failure avoided:** every chain this session pushed its worktree branch and every one was
merged the same day. No session output left invisible.

## 2 · Verification

| | Result |
|---|---|
| Frontend suite | **203 passed / 22 files / 0 failed** |
| Backend suite | **720 passed / 8 skipped / 1 failed** in 6m11s. The single failure is `test_mailbox_health`, red **by design** — see §4 |
| Mailbox | **FAILS** on a real, deliberate collision — §4 |

**Spot-check, not accepted on report:** the per-league export fix was verified by listing each league
directory and confirming none is missing a required artifact. 7 files each where there had been 3–4.

**Trap worth recording:** the backend suite reports **10 collection errors under system `python3`**
because `pandas` is absent there. It is not a red suite — it is the wrong interpreter. Use
`.venv/bin/python`. The PM hit this during this very closeout.

## 3 · Defect register — everything the founder personally reported

| # | Defect | Fixed? | Reported before? |
|---|---|---|---|
| 1 | Cannot switch leagues — data will not load | **Yes**, verified | No |
| 2 | ADP not shown anywhere | **Yes**, on all three screens | No |
| 3 | Opponents not usable in a live draft | **No** — FR-032, open | No |
| 4 | Site unreachable on mobile | **Yes** — wrong hostname; only `draft.` was attached | No |
| 5 | Cloudflare login sending codes that never worked | **Yes** — an Access app with no policy denies everyone | No |
| 6 | Build failing on deploy | **Yes** — `NODE_VERSION` was `2`, not `22` | No |
| 7 | "Refresh data" fails on the live site | **No** — recorded; correct behaviour, wrong surface | No |

**Repeat rate: 0.** Nothing he reported had been reported before.

**Detection split — the number that matters, and it got worse before it got better.** He personally
caught, beyond the defects above: that the effort scale had two tiers above the one being used; that
`high` is a default rather than a ceiling; that market ADP is not expert consensus; that bottom-up
does not need consensus history to be built; that the quarterback result contradicted the project's
own research; and that the screenshot blocker had been solved hours earlier. **Six substantive
findings from the founder in one session.** That is not a healthy ratio and it is the standing
blindness this project already records.

**Interrupts, by category:**

- **Permission prompts: 0.** The hook and all approval rules were removed today.
- **Judgment questions: several**, mostly correct — the blend-versus-separate rule, the public-site
  question, the merge to `main`. Each is a decision he reserved.
- **Genuine escalations: 2** — the source authorisations being scoped "private, one person" against a
  now-public site, and the `CLAUDE.md` §4 conflict with his preferred product shape.

## 4 · Mailbox

**Fails, and the failure is the system working.** `ADR-054` and `ADR-055` each exist twice — once on
`main`, once on the unreviewed `mock-calibration-kickers` branch. The duplicate-ID backstop built
today caught both on its first run. **Left unresolved deliberately**: renumbering is the merger's
decision, and silently fixing it would hide the collision from whoever merges.

**43 threads open**, down from 47. Five closed on evidence — real screenshots, not test counts.

## 5 · Self-audit — what I asserted that turned out to be wrong

This is the section with value, so it is the longest.

1. **"I'll set effort per dispatch."** I cannot; the tool exposes model only. Claimed before checking,
   corrected within the hour. **Retracted in place.**
2. **"Strategist runs at high effort"**, said as though elevated. `high` is the default. Two tiers
   above it were unused across every role in the project until he asked.
3. **"Worktrees are obsolete in the cloud."** Half true — the database and dev-server reasons died,
   the concurrency reason moved from session level to agent level. Removing the discipline caused a
   real failure within hours.
4. **"Market ADP can substitute for consensus."** It cannot. Different quantities. He corrected me.
5. **"The four-season limit is a hard wall."** True of consensus, false as stated — bottom-up needs
   no consensus history to be built or scored. He corrected me.
6. **Manufactured a phantom collision twice**, committing running agents' files. The second cost a
   chain a full decision cycle. Then **broke the same rule again one commit after tightening it**, by
   `git add`-ing a directory containing three agent worktrees.
7. **Over-read "optimize for phone viewing right now"** as build responsive layouts. Roughly a third
   of the largest agent run of the day was spent on work he then cancelled. *"Right now" is urgency,
   not scope.*
8. **Dispatched three Fable mandates mid-week** against a budget he spends at week's end. All three
   killed.

**Stale claims found and corrected today:** `CURRENT-STATE` said FFC was blocked (unblocked that
morning); said the automated capture had succeeded on schedule (it had never run on schedule); said
the Windows task was redundant (it was not); listed the Predictions tab as absent (it exists);
`handoffs/README` said design could not read the repo (it could, for two days).

**Tickets older than two rounds:** the design-fidelity set, deliberately paused. Most of the deferred
list is PM-generated — worth saying whenever it is defended.

## 6 · What was cut

Per the cost-discipline rule, naming it: **stash resolution, worktree database copying and
dev-server sprawl were not run.** They no longer apply — there were no stashes, no shared checkout,
and worktrees are per-agent and disposable. That is the closeout getting shorter because collisions
dropped out, which is the intended direction.

## 7 · Standing items

- **First genuinely scheduled ADP capture fires 09:15 UTC tomorrow.** Until it does, the Windows task
  stays. Check the run's `event`, not the commit author — that is how this was got wrong once.
- **The site is public without a password**, by his decision, while every source authorisation is
  scoped to private single-person use. He will add it in a day or two.
- **Fable runs end of week, at max**, on the bottom-up rankings, carrying today's addendum.
