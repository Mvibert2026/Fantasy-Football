# PR-007 — F-RECOMMENDATION-CONSTANTS: recommendation-constants ablation

**Registered by `strategist`, 2026-07-29, before any arm was fitted.** Pre-commitment:
`docs/preregistration/PR-007-recommendation-constants-ablation.md` (frozen
`content_hash: sha256:bdcd090e4487aaf6032d9f6dbe3167254bb691ad76a6514795667413763f6a3c`, verified
unmodified before this run). Run by `backend`, 2026-08-01.

## m_b = 4

This is **not** a factor-screen batch in the sense the rest of this manifest's files are (a
walk-forward component-prediction sweep) — it is a separate registered family
(`F-RECOMMENDATION-CONSTANTS`, its own `docs/preregistration/families/F-RECOMMENDATION-CONSTANTS.yaml`,
`m: 4`) evaluating the draft-room recommender against a full draft-simulation metric, not a
walk-forward MAE/rho metric. Recorded here anyway per `CLAUDE.md`'s standing rule that the
multiple-comparisons correction is computed against the campaign, never a batch in isolation, and
per PR-007 §5's own explicit multiplicity design (m fixed at 4, BH not applicable at n=3 seasons
— see PR-007 §5 for the full reasoning; nothing here overrides that).

The four registered comparisons (`F-RECOMMENDATION-CONSTANTS.yaml`):

| # | comparison | tests |
|---|---|---|
| 1 | `FULL - PLAIN` (does the shipped recommender beat plain VBD) | 1 |
| 2 | `FULL - LOO(need+8)` | 1 |
| 3 | `FULL - LOO(te+18)` | 1 |
| 4 | `FULL - LOO(qb-25)` | 1 |

No p-value/BH correction is applied within this family (PR-007 §5: n=3 seasons, sign-test floor
p=0.25, no admissible p-value at the registered resampling unit). The family's own structural
controls (fixed m, unanimity-across-9-cells requirement, conjunctive non-denominator criteria
(d)-(g), PROVISIONAL labelling of any KEEP) stand in place of BH, as registered in advance.

Descriptive arms (`vbd_te_window`, `vbd_need_continuous`, `bpa_consensus`, `balanced`, the
sigma=0 diagnostic, and the pick-flip diagnostic) are outside the family, carry no CI/p-value, and
do not enter `m_b`.

## Outcome

See `docs/ranking/pr-007-results.md` for the full results (census, per-comparison margins,
criteria (a)-(g), pick-flip diagnostic, QB rank-curve slopes) and `docs/preregistration/test_run_log.jsonl`
for the logged run entries.
