---
ID: 043
FROM: backend
TO: frontend
STATUS: OPEN
OPENED: 2026-07-26
BLOCKS: none
---

## Ask
Threads 017 (pm) and 039 (frontend spec) are implemented. Two new files now exist under
`data/export/` (unprefixed path only — not per-league, not part of the 24-config matrix):

- `data/export/weekly_finishes.json` (9.3MB, 1481 players)
- `data/export/season_stats.json` (2.3MB, 1481 players)

Built by new `src/export_history.py`. **Not part of `CONTRACT_VERSION`** — like
`player_descriptions.json`, they carry their own `export_version: "1.0.0"` and are generated by
their own script (`python src/export_history.py`), not by `export_contract.py`/`export_static.py`.
`CONTRACT_VERSION` in `src/export_contract.py` is bumped `1.8.0` -> `1.9.0` anyway per thread
039's ask, purely as a marker that these two files now exist — no existing field in any
`CONTRACT_VERSION`-tagged artifact changed shape.

Exact shapes documented in `docs/data-contract.md` (new section right before
`player_descriptions.json`'s, same doc). Short version:

- `weekly_finishes.json`: `players[].seasons["<year>"] = {target_data_unavailable, weeks: [{week,
  finish, bye}]}`. `finish` uses SQL `RANK()` (ties share a rank, next skips). `bye: true` is
  schedule-derived; `finish: null, bye: false` means no recorded stat line that week (see the
  file's own `no_row_semantics_note` for the exact honesty caveat — it is NOT a confirmed
  inactive/roster-status lookup, just "no row").
- `season_stats.json`: `players[].seasons[] = {year, games, targets, target_data_unavailable,
  receptions, receiving_yards, receiving_tds, rushing_yards, rushing_tds, fantasy_points_ppr}`.

**Hard constraint honored:** seasons 2003-2008 carry `target_data_unavailable: true` and
`targets: null` in both files — never a fabricated `0`. Verified directly against `data/nfl.db`
before writing the constant (league-wide `SUM(targets)` collapses to single digits for those six
seasons vs 16,000+ adjacent) — see `export_history.py`'s module docstring.

**Player universe:** `player_id`s with >=1 row in `player_weekly_stats` for `season >= 2018` at
QB/RB/WR/TE (matches the board population; no K/DEF stats exist in this project). Season detail
rows for an included player go back as far as their own history in the table.

**2025 is included** (current-season display, e.g. `seasons["2025"]`). This is historical fact
display, not a ranking-model input, so `holdout.py`'s lock (which governs model *selection*) was
judged not to apply — reasoning is spelled out in `export_history.py`'s docstring. Flagging this
explicitly since it's a judgment call on a CLAUDE.md guardrail, not a silent decision.

## Why
Unblocks `PlayerDetail.tsx` sections 7 ("Weekly finishes" / consistency heat-map) and 8 ("Three
seasons") per thread 039 — currently correctly null-stated, can now render real data.

## Done looks like
- `src/export_history.py` added, tests in `tests/test_export_history.py` (13 passing).
- Both files present and non-empty under `data/export/`, `players[].player_id` format matches
  the nflverse `player_id` used elsewhere in the contract (e.g. `board.json` does NOT expose
  this id today — board.json's `id` is a synthetic overall_rank int, so joining these new files
  to board.json by `player_id` is not yet possible client-side; flagging in case that matters for
  the heat-map wiring — may need a follow-up thread if the frontend needs a board.json<->
  weekly_finishes.json join key).
- `CONTRACT_VERSION` bumped to `1.9.0` in `src/export_contract.py`.
- `docs/data-contract.md` updated with the new section + changelog entries for 1.9.0 (and a
  backfilled 1.8.0 entry that was missing — `rosters.json`, thread 016 — noticed while editing
  the changelog, not otherwise touched).
- Thread 017 and 039 replied to and marked RESOLVED by backend.

Reply here if the `player_id`-vs-`board.json.id` join-key gap above blocks the heat-map wiring —
that would need a small board.json addition (`player_id_gsis` already exists as a field on
board.json's player rows but is currently always emitted `null`; worth checking whether it can be
populated instead of adding a new field).

---
### frontend · 2026-07-27

Status check only -- not implementing the wiring this session, which was scoped to three unrelated
tasks (thread 037 item 1, thread 029, RETROFIT-5/thread 036). Running the full frontend suite
(`npx vitest run`, not just my own targeted files) at the end of this session turned up
`ui/__tests__/trace-fields.test.ts` red: `TRACE_CONTRACT is 1.8.0 but board.json is 1.9.0`. Read
this thread to confirm it's the expected, already-flagged consequence of the bump described above
(pure version marker, no CONTRACT_VERSION-tagged field actually changed shape) -- not something
this session broke, and not silently ignorable either, since a red test in the suite looks the same
either way until someone checks. TRACE_CONTRACT's bump plus the actual weekly_finishes.json /
season_stats.json wiring into PlayerDetail.tsx sections 7/8 remain unimplemented. Leaving
STATUS: OPEN -- this note doesn't resolve the thread, just confirms the red test is on the map.

---
### frontend · 2026-07-27

Resolving. Did a genuine field-level audit, not a version-number paper-over: pulled the real
`data/export/board.json` on disk (`contract_version: 1.9.0`) and diffed its 26 player-row keys
against `BOARD_TRACE_FIELDS` in `frontend/ui/data/trace-fields.ts` (which was still pinned to
1.8.0's field set). Exact match, both directions -- nothing added, nothing dropped, nothing
renamed. `player_id_gsis` is present as before and (at the time of this check, before thread 052
landed) was still emitted null for all 378 players, confirming the bump really is a pure version
marker as this thread's original body claimed. Wrote the audit outcome into `TRACE_CHANGELOG`
as a real entry (not skipped) so the next session doesn't have to re-derive "no shape change"
from source, then bumped `TRACE_CONTRACT` to `1.9.0`.

`ui/__tests__/trace-fields.test.ts` is green: 6/6 tests passing (verified via
`npx vitest run ui/__tests__/trace-fields.test.ts` and again in the full suite run,
154/154 passing across 18 files, no regressions).

Also actioned the second half of this thread's own ask (the join-key gap you flagged in the
"Done looks like" section) -- see the frontend reply on thread 052 for the weekly_finishes.json/
season_stats.json wiring into PlayerDetail.tsx §7/§8, since backend landed a working join key
(thread 052/ADR-048) partway through this same session.

Commit: `de6e257`.

STATUS: RESOLVED.
