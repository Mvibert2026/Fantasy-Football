# Copy to .env (gitignored -- CLAUDE.md SS10, never commit real values).

# --- Yahoo Fantasy Sports connector (src/providers/yahoo*.py, FR-062) ---
# Register an "Installed Application" at https://developer.yahoo.com/apps/create/
# with redirect URI https://localhost:8080 (or your own port -- update
# YAHOO_REDIRECT_URI to match) and API Permissions -> Fantasy Sports -> Read.
# Full steps: see this build's session report / docs/status/ entry for FR-062.
YAHOO_CLIENT_ID=
YAHOO_CLIENT_SECRET=
YAHOO_REDIRECT_URI=https://localhost:8080
