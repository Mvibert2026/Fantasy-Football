# Anthropic key for the reasoning lane only. Never committed; never bundled into the client.
# The FantasyPros credential used by the backend is a different secret and does not belong here.
ANTHROPIC_API_KEY=
