[pytest]
markers =
    network: tests that hit the network (nflverse data pulls)
    requires_db: tests that read the already-ingested local data/nfl.db (no network, but needs it populated)
testpaths = tests
