# Rebuildability

Rankings history 2021–2025 is unrecoverable — the DynastyProcess mirror serves only the
current scrape. Verified per season with the ingester's own `resolve_snapshot_date`: 2021–2025
all fail, only 2026 resolves.
