# Capture status, document B

A scheduled run has never fired. Exactly one run exists in the repository's entire history and
it was a manual dispatch. Do not disable the local Windows Scheduled Task.
