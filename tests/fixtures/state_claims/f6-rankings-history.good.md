# Rebuildability

The DynastyProcess mirror does still serve 2021–2025 ECR snapshots, and a fresh
`ingest_rankings.py` pull reproduces the committed rankings history row-for-row (2,540 rows,
14 columns, zero differences).
